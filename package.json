{
  "name": "projekt",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:api": "turbo run dev --filter=api",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{js,jsx,vue,ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "eslint": "^8.56.0",
    "prettier": "latest",
    "turbo": "latest",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@8.13.1"
}
